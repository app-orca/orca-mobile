import {StyleSheet} from 'react-native';
import styles from '../../../styles';

export default StyleSheet.create({
  content: {
    ...styles.flexOne,
  },
  descriptionCard: {
    marginHorizontal: 20,
    marginBottom: 20,
  },
});
